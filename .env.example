# Скопируй этот файл в .env и впиши свои токены.
# Файл .env в репозиторий НЕ попадает (он в .gitignore).

# Токен API МойСклад (Настройки → Доступ к API)
MOYSKLAD_TOKEN=

# GitHub Personal Access Token с правом записи в репозиторий (для загрузки фото в gh-pages)
GITHUB_TOKEN=
